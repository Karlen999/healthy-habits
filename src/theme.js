import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: { main: "#2e7d32" },  // leafy green = health vibe
    secondary: { main: "#0288d1" } // fresh blue
  },
});
